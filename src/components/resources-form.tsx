import { SubscribeForm } from ".";

const ResourcesForm = () => {
    return (
        <div>
            <SubscribeForm
                textLg='Free resources for you!'
                textSm='Enter your email into form below to get updates from us'
                textBtn='Submit'
                variant='dark'
            />
        </div>
    );
};

export default ResourcesForm;
