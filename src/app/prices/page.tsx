import { Plans, Services } from "@/components";
import React from "react";

const Prices = () => {
    return (
        <main>
            <Plans />
            <Services />
        </main>
    );
};

export default Prices;
