import { Customers, Hero, Offices } from "@/components";

export default function Home() {
    return (
        <main>
            <Hero />
            <Customers />
            <Offices />
        </main>
    );
}
